export type Task = {
    id: number,
    title: string,
    description: string,
    priority: number,
    completed: boolean,
    stage_name: string,
    due_date: string,
    board_name: string,
    created_date: string
}
